server:
  port: 8080

spring:
  profiles:
    active: deepseek,h2  # 默认使用DeepSeek + H2数据库，可改为 ollama,h2 使用离线模式
  
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: qwen2.5:7b-instruct
    
    # OpenAI 配置（仅用于提供 API Key，实际 Base URL 在代码中指定）
    openai:
      api-key: ${OPENAI_API_KEY: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}
  
  # JPA Configuration (通用配置)
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# Chat Configuration
chat:
  max-context-messages: 10  # 保留最近10轮对话作为上下文
